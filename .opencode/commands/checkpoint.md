---
description: Persist a compact handoff before ending or replacing the current session.
agent: builder
---

Do not start new implementation work. Create a durable handoff for the current task.

Update `TASK.md` with the current status, completed and pending plan items, changed paths, concise verification outcomes, blockers, and one exact next action. Refresh `.opencode/state/CONTEXT.md` only if its facts are stale. Remove raw logs, obsolete exploration, and conversational history. Confirm when a fresh session can continue with `/continue-task`.

Additional note: $ARGUMENTS
