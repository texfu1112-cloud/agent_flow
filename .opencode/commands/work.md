---
description: Run a request through the stateful multi-agent workflow.
agent: builder
---

Execute this request end to end using the Builder workflow:

$ARGUMENTS

Treat this command invocation as the authoritative user requirement. Keep Sol inputs bounded, persist every phase transition in `TASK.md`, and finish with verified code or a precise blocker.
