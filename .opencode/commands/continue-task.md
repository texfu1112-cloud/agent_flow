---
description: Resume the active task from durable project state in a fresh session.
agent: builder
---

Resume the active task without relying on prior chat history.

Read `TASK.md` first, then `.opencode/state/CONTEXT.md`, the current working tree, and only the architecture or decision entries referenced by the task. Validate that recorded state still matches the files, continue from `Next Action`, and update the checkpoint before responding.

Optional user guidance: $ARGUMENTS
